import ChangeAccountBalance from "./updown";
import { combineReducers } from "redux";
const rootReducer =  combineReducers({
    ChangeAccountBalance : ChangeAccountBalance,
})
export default rootReducer;